# Повторный свежий lifecycle-аудит netOS

Статус: **ЗАВЕРШЁН — PASS**. Прогон выполнен 2026-09-01 18:00–18:49 MSK на разрешённом dev-сервере. Предыдущие отчёты и артефакты не использовались как доказательства.

## Итог

- Все пять дефектов из `audit_fresh_20260901.md` исправлены и независимо воспроизведены на свежей сборке: DNS provider/DNSSEC, audit после restore, синхронизация UI после restore, грязный draft после manual/timeout rollback и Kea lease после uninstall.
- Наблюдение DDNS с нулевыми timestamp также исправлено и проверено через JSON и UI.
- Новых дефектов продукта в повторном прогоне не найдено.
- Финальный полный uninstall прошёл успешно. Продукт, данные, backup-каталог, юнит, CLI, completion, процессы, сокеты, интерфейсы, внешние component-бинарники и `/var/lib/kea/netos-leases4.csv` отсутствуют.
- SSH, `eth0`, исходный default route и machine-id сохранены; failed units — 0; пакетный состав и manual marks идентичны pre-install baseline.

## Идентичность и сборка

- Target/resolved/remote: `45.38.170.119`; SSH user `root`; hostname `netos`.
- Debian 13.5 (trixie), `x86_64`; commit `b69e46b27d9286cea85e0b318f0c27a260aae50d` плюс текущие рабочие изменения.
- Machine-id SHA-256: `e4c0e747159c4ad0a03094250f5d0ba1f973e5574ab948160b2bf73149a340bb` до и после цикла.
- Frontend: `tsc --noEmit` и production Vite build — PASS. `go vet ./...` — PASS.
- Все новые targeted Go-тесты API/manage/DDNS — PASS. Общий Windows `go test ./...` дошёл до известных platform-specific TempDir cleanup races и существующего ожидания непустой statistics series; затронутые изменениями пакеты и новые сценарии проходят отдельно.
- Linux static binary SHA-256: `7019289dbbba09e3eb8a3dad56550aa74f601d2647541019fa39661783c78a59`.
- Source archive SHA-256: `d461bebc5544e5279c32378aab2105c6607d08f7bf559e265f67fea6e698577d`.

## Покрытие lifecycle

### Clean install и компоненты

- Pre-install clean probe и manifest-backed snapshot — PASS: продукта, юнита, listener и процессов не было; SSH/default route исправны; failed units 0.
- Installer отказал при отсутствующем checksum и при неверном checksum, выполнив rollback до запуска бинарника.
- Установка с корректным checksum — PASS: версия `rerun-b69e46b2`, remote binary hash совпал, unit active/enabled, readiness `1`, listener `*:8443`, initial credentials `0600 root:root`.
- После первого login initial credentials удалены.
- Через реальный UI выбраны все 16 компонентов; план показал 15 установок, поскольку QoS уже был готов. Все 16 получили состояние «готов».
- После reset компонентный набор стал пустым, Kea lease отсутствовал; post-reset повторная установка тем же installer сохранила factory config и учётную запись.

### Auth, API и CLI

- Свежий live API sweep: **56/56 PASS** — public/auth/session/logout, CSRF, роли, negative JSON, literal confirmations, status/catalog/clients/interfaces/leases/ARP/routes/audit/revisions/statistics, key generation/derivation, validate, idempotence, stale conflict и render.
- CLI PASS: version/status/logs/plan/completion, restore на EOF без мутации, render list и все 16 artifacts (`iptables`, WireGuard, Xray, Xray servers, hostapd, ocserv, strongSwan, dnsmasq, ISC, Kea, Unbound, dnsproxy, resolv, network, sysctl, config).
- Password reset через stdin не раскрыл пароль и отозвал активную browser session; повторный login прошёл.
- Same-version update корректно вернул no-op; повторный запуск того же installer сохранил 16 компонентов, config и session.

### UI и исправленные сценарии

- Headed Chromium прошёл Dashboard, Devices, Network, Routing, Channels, VPN, Wi-Fi, QoS/DDNS, Firewall, DHCP/DNS, Components, System, History и Diagnostics; для каждого раздела сохранён full-page screenshot.
- System/dark/light cycle, мобильный viewport 390×844, mobile menu/Escape и возврат к desktop — PASS.
- DNS regression: исходная модель имела пустой provider и визуальный placeholder; включение resolver атомарно выставило `provider=dnsmasq`, `dnssec=false`, `problems=0`. Скрытого невалидного draft больше нет.
- DDNS regression: до первой попытки `/api/ddns/status` содержит только `enabled` и `success`; `last_attempt`, `last_success` и `error` отсутствуют. UI не показывает 01.01/год 0001 или ложную ошибку.
- Console events классифицированы: 401 до login/после session revocation и `ERR_CONNECTION_CLOSED` во время штатных restart/backup/restore/reset. Необъяснённых JS exception нет; autosave после restore сам восстановился после ожидаемого stale CSRF/version ответа.

### Apply, rollback, backup/restore и faults

- Safe apply и рискованный apply+confirm — PASS.
- Manual rollback: active config вернулся, `pending=false`, `rollback=manual`, главное — `dirty=false`.
- Timeout rollback после 30 секунд: active config вернулся, `pending=false`, `rollback=timeout`, `dirty=false`.
- Backup create/list/download — PASS. После временного изменения hostname restore вернул `netos`; UI обновился без reload пользователя.
- В восстановленной БД присутствует успешная запись `restore`, user `system`, с именем backup — запись больше не исчезает вместе с заменённой БД.
- Backup delete — PASS. `reset --yes --backup` создал копию и вернул factory config (`components=[]`, DNS off, clean draft).
- `netos stop/start/restart` — PASS. Два контролируемых SIGKILL дали новые PID и `NRestarts` 0→1→2; unit/listener восстановились, failed units остались 0.
- Непрерывный монитор: 2162 samples, max memory 381456384 B, max restarts 2, max failed units 0, minimum available disk 4275150848 B.

## Ограничения среды

- На VM одна NIC и нет Wi-Fi radio/клиента; физический Wi-Fi association и настоящий multi-NIC failover не применимы. Их безопасные substitutes — UI conditional states, validation, render и component install.
- Публичный URL из локального браузера перехватывался Kaspersky с HTTP 499. Тот же живой сервер проверен через SSH TCP tunnel к публичному `:8443`; это ограничение локальной среды, не netOS.
- ACME с реальным доменом, внешний DDNS update и VPN handshake с независимым клиентом не выполнялись без внешних DNS/provider credentials; negative/validation/render/key-generation пути покрыты.

## Финальное удаление

- Перед uninstall создан точный fixture `/var/lib/kea/netos-leases4.csv`; после full uninstall он отсутствует.
- PASS absent: `/usr/local/bin/netos{,d}`, `/usr/local/bin/{dnsproxy,xray}`, unit, completion, `/var/lib/netos`, `/etc/netos`, `/var/log/netos`, baseline, backups, `/opt/netos*`, ready markers и Kea fixture.
- PASS: процессы/listeners/interfaces/netos units/component commands/run-specific remote temp отсутствуют.
- PASS: packages added/removed = 0; manual marks added/removed = 0; failed units 0; SSH active; `eth0` UP; default route остался `via 45.38.170.1 dev eth0`; disk 54%.
- Локальные temporary credentials, reset secret output, downloaded backup, browser session state и оба TCP tunnel удалены после проверки.

## Evidence

- `output/netos-rerun-20260901-180045-evidence.tar.gz` — `c57cab4ba6abc894178f03593f1ea7c2769c98c9501c00527f5d8d206a9bbb70`.
- `output/netos-rerun-20260901-180045-cli.tar.gz` — `848c049ac807c27f214a1a1e5a8fc4036e06f6fe2e625d24b6107ab76845447b`.
- `output/netos-rerun-20260901-180045-final-proof.txt` — независимый `FINAL_RESULT=PASS`.
- `output/playwright/netos-rerun-20260901-180045/` и `output/playwright/netos-rerun-20260901-180045-*.yml` — snapshots, screenshots и классифицированный console log.

Все remote QA/proof-файлы удалены после скачивания evidence; сервер оставлен чистым.
